import Vue from 'vue';

new Vue({
    el: '#app',
    data() {
        return {
            message: 'Tailwind Template',
            versionMessage: 'Using Tailwind {version}',
            version: '1.0.4',
        };
    },
});
